[package]
name = "easy-local-mcp-tray"
version = "0.3.9"
description = "Native tray shell for Easy Local MCP Control Center"
edition = "2021"
license = "MIT"

[build-dependencies]
tauri-build = { version = "2" }

[dependencies]
tauri = { version = "2.11.5", features = ["tray-icon"] }
url = "2.5"
